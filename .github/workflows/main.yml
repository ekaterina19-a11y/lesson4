name: My First Action
on: [push] 

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4 

      - name: Say Hello
        run: echo "GitHub Actions успешно активированы в репозитории lesson4!"
